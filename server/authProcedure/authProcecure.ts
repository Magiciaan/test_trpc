import { z } from "zod";
import { publicProcedure } from "../utils/trpc";

// export const authorizedProcedure = publicProcedure.input(z.object({ accessToken: z.string() })).use((opts) => {
//     const authorizeUser =
//     if () {

//     }
// })
